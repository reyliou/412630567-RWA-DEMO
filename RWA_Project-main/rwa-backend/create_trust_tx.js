const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://postgres.uowremtggfpoxxruiccw:newsun87S6202963@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?pgbouncer=true', ssl: { rejectUnauthorized: false } }); client.connect().then(() => client.query('CREATE TABLE IF NOT EXISTS public.bank_trust_transactions ( id SERIAL PRIMARY KEY, trust_account_id integer NOT NULL, tx_type character varying NOT NULL, amount numeric NOT NULL, reference_note text, recorded_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP );')).then(() => { console.log('Table created!'); return client.end(); }).catch(console.error);
